import {} from "../actions/branchSummaryActions";
import {} from "../../logic/branchSummaryManager";
import {fromJS} from "immutable";

const initialState = {
};

export default function (state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

